---
layout: layout.njk
pageTitle: 2026 Rochester, MN Bag Tag
---
<header class="page-header hero panel stack">
  <p class="eyebrow">Rochester, MN</p>
  <h1>{{ site.title }}</h1>
</header>

{% if publicModel.homepage.leaderboardRows.length %}
  <section class="panel stack">
    <div class="section-heading stack-tight">
      <p class="eyebrow">Season Race</p>
      <h2>Leaderboard</h2>
    </div>
    <div class="leaderboard-table-scroll">
      <table class="leaderboard-table">
        <thead>
          <tr>
            <th scope="col" class="season-standing">Season Standing</th>
            <th scope="col" class="leaderboard-season-total">Season<br />Total</th>
            <th scope="col">Player</th>
            {% for event in publicModel.homepage.leaderboardEvents %}
              <th scope="col" aria-label="{{ event.accessibleLabel }}">{{ event.shortDate }}</th>
            {% endfor %}
          </tr>
        </thead>
        <tbody>
          {% for row in publicModel.homepage.leaderboardRows %}
            <tr>
              <td class="season-standing">{{ row.seasonStanding }}</td>
              <td class="leaderboard-total-cell leaderboard-season-total">
                <span class="leaderboard-points-value">{{ row.seasonPoints }}</span>
                <span class="leaderboard-points-label">pts</span>
              </td>
              <th scope="row" class="leaderboard-player-cell">
                <details class="leaderboard-breakdown-toggle">
                  <summary class="leaderboard-summary">
                    <span class="leaderboard-name">{{ row.playerName }}</span>
                    <span class="leaderboard-summary-indicator" aria-hidden="true">
                      <span class="leaderboard-summary-closed">Show breakdown</span>
                      <span class="leaderboard-summary-open">Hide breakdown</span>
                    </span>
                  </summary>
                  <span class="leaderboard-events">{{ row.eventsPlayed }} event{% if row.eventsPlayed !== 1 %}s{% endif %}</span>
                </details>
              </th>
              {% for event in row.eventOverview %}
                <td data-event-slug="{{ event.eventSlug }}">{% if event.played %}{{ event.points }}{% endif %}</td>
              {% endfor %}
            </tr>
            <tr class="leaderboard-breakdown-row">
              <td colspan="{{ 3 + publicModel.homepage.leaderboardEvents.length }}">
                <div class="leaderboard-breakdown">
                  <div class="table-scroll">
                    <table>
                      <caption class="visually-hidden">{{ row.playerName }} scoring breakdown</caption>
                      <thead>
                        <tr>
                          <th scope="col">Event</th>
                          <th scope="col">Attendance</th>
                          <th scope="col">Placement</th>
                          <th scope="col">Starting Tag Bonus</th>
                          <th scope="col">Tag 1 Bonus</th>
                          <th scope="col">Beat Your Tag Bonus</th>
                          <th scope="col">Total</th>
                        </tr>
                      </thead>
                      <tbody>
                        {% for event in row.eventBreakdown %}
                          <tr>
                            <td><a href="/events/{{ event.eventSlug }}/">{{ event.eventName }}</a></td>
                            <td>{{ event.attendance }}</td>
                            <td>{{ event.placement }}</td>
                            <td>{{ event.startingTagBonusDisplay }}</td>
                            <td>{{ event.tagOneBonusDisplay }}</td>
                            <td>{{ event.beatYourTagBonusDisplay }}</td>
                            <td>{{ event.eventTotal }}</td>
                          </tr>
                        {% endfor %}
                        <tr>
                          <th scope="row">Totals</th>
                          <td>{{ row.totals.attendance }}</td>
                          <td>{{ row.totals.placement }}</td>
                          <td>{{ row.totals.startingTagBonus }}</td>
                          <td>{{ row.totals.tagOneBonus }}</td>
                          <td>{{ row.totals.beatYourTagBonus }}</td>
                          <td>{{ row.totals.eventTotal }}</td>
                        </tr>
                      </tbody>
                    </table>
                  </div>
                </div>
              </td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </section>
{% endif %}

{% include "points-rules-summary.njk" %}

<section class="panel stack">
  <div class="section-heading stack-tight">
    <h2>Remaining Schedule</h2>
  </div>
  <ul class="event-list">
    <li class="event-tile panel stack-tight">
      <span class="event-date">August 2</span>
      <span>Driftless (Major)</span>
    </li>
    <li class="event-tile panel stack-tight">
      <span class="event-date">August 16</span>
      <span>Spring Valley</span>
    </li>
    <li class="event-tile panel stack-tight">
      <span class="event-date">September 6</span>
      <span>Mantorville</span>
    </li>
    <li class="event-tile panel stack-tight">
      <span class="event-date">September 27</span>
      <span>St Charles</span>
    </li>
    <li class="event-tile panel stack-tight">
      <span class="event-date">October 11</span>
      <span>Championship, Driftless</span>
    </li>
  </ul>
</section>

{% if publicModel.homepage.events.length %}
  <section class="panel stack">
    <div class="section-heading stack-tight">
      <h2>Past Rounds</h2>
    </div>
    <ul class="event-list">
      {% for event in publicModel.homepage.events %}
        <li class="event-tile panel stack-tight">
          <span class="event-date">{{ event.eventDate }}</span>
          <a href="/events/{{ event.slug }}/">{{ event.name }}</a>
        </li>
      {% endfor %}
    </ul>
  </section>
{% else %}
  <p class="empty-state">No events yet.</p>
{% endif %}
